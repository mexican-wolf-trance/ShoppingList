<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <androidx.recyclerview.widget.RecyclerView
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/shopping_recycler_view"
        android:name="edu.charles_wyatt.shoppinglist.ViewListFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="16dp"
        app:layoutManager="LinearLayoutManager"
        tools:context=".ui.viewshoppinglist.ViewListFragment"
        tools:listitem="@layout/fragment_view_list" >
    </androidx.recyclerview.widget.RecyclerView>

    <Button
        android:id="@+id/add_new_list_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/add_list_btn"
        android:layout_alignBottom="@id/shopping_recycler_view">
    </Button>

</RelativeLayout>